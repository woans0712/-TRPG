// MessengerR-only bot scaffold.
// All previous commands and features were intentionally removed.

function response(room, msg, sender, isGroupChat, replier, imageDB, packageName) {
  // Keep this empty. New MessengerR features will be added here one by one.
}
